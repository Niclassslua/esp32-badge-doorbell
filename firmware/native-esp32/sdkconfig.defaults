CONFIG_ESPTOOLPY_FLASHSIZE_16MB=y
CONFIG_ESPTOOLPY_FLASHSIZE="16MB"
CONFIG_ESPTOOLPY_FLASHMODE_DIO=y
CONFIG_ESPTOOLPY_FLASHFREQ_80M=y

# WiFi + OTA + log-ship init allocates ~8 KB of stack frames
CONFIG_ESP_MAIN_TASK_STACK_SIZE=10240

# Enable DEBUG-level log output so log_ship captures maximum detail.
# Internal ESP-IDF driver noise is suppressed to WARN at runtime in log_ship_init().
CONFIG_LOG_DEFAULT_LEVEL_DEBUG=y
CONFIG_LOG_DEFAULT_LEVEL=4
CONFIG_LOG_MAXIMUM_LEVEL=4

# Use the OTA-capable custom partition table
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions_ota.csv"

# Roll bad app images back to the recovery slot if they fail before marking
# themselves valid.
CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y

# Power saving: dynamic frequency scaling lets idle/event-driven firmware
# drop CPU/APB clocks between WiFi, HTTP, touch, and display work.
CONFIG_PM_ENABLE=y
